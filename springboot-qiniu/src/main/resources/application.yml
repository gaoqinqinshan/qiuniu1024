# 端口
server:
  port: 8081
# mysql配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qiniu?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: word
    driver-class-name: com.mysql.cj.jdbc.Driver
